1. What is the difference between var, let, and const?
    Ans: var,let and const are used to declare variables. var is scoped fuction. it can be redeclare and update.
        let is block scoped. it can be update but cannot redeclare. const is block scoped. but i cannot be update and redeclare.

2.  What is the spread operator (...)?
    Ans: the spread sperator in javascript is used to expand elements of an array or objects into individual elements.

3.  What is the difference between map(), filter(), and forEach()?
    Ans: map creates an array after applaying a function to each element.
        filter creates a new array with elements that pass a condition.
        forEach loops through each element but does not return a new array.

4.  What is an arrow function?
    Ans: An arrow function is a shorter way to write a fuction in javascript using => this.

5. What are template literals?
    Ans: template literals are a way to create strings in javascript using backticks (`).